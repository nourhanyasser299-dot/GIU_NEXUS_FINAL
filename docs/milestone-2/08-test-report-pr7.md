# PR #7 — pre-merge fixes E2E test report

**Repo / branch / commit:** `HusseinSelim-1977/Nourhan_Project`, `feature/hf-job-summarization` @ `e04c17a`
**PR:** https://github.com/HusseinSelim-1977/Nourhan_Project/pull/7
**Driver:** Playwright over Chrome CDP at `http://localhost:29229`, real Mongo (in-memory) seeded with 3 jobs, real Hugging Face Inference router (no mocks), JWT-gated routes hit live.
**Result:** **7/7 passed.**

## Scope (the four pre-merge fixes only)

1. Gate `POST /api/ai/skills/extract`, `/match`, `/summarize` behind JWT.
2. Remove the three hardcoded "UX Visionary" / "Frontend Architect" / "Data Scientist" demo cards from `#job-grid`.
3. Add the recruiter `company` input on the auth form (hidden for seekers, required + persisted for recruiters).
4. Replace the broken `motion@latest/dist/motion.js` import with a hardened `motion@10.18.0/+esm` dynamic import.

## Bullet summary (one line per assertion)

- T1: motion ESM import works, no legacy import, **0** motion-related console errors — **passed**
- T2: jobs board renders **3** dynamic cards (each with a Mongo `_id`); 0 no-arg `openJobModal()` cards; no demo titles in DOM — **passed**
- T3a: seeker register — `auth-company-group` `display:none`, `auth-name-group` visible — **passed**
- T3b: seeker `POST /api/auth/register` 201 with `role:"seeker"` and **no** `company` key — **passed**
- T4-pre: recruiter register — `auth-company-group` `display:flex` — **passed**
- T4a: empty company → `#auth-company.error`, `#company-error.visible`, **0** register requests fired — **passed**
- T4b: filled company → `POST /api/auth/register` 201 with `company:"Adversary Holdings Ltd"` and `role:"recruiter"` — **passed**
- T5: anonymous `POST` to all 3 AI routes → `401 "Authentication required"` — **passed**
- T6: authenticated `POST` to all 3 AI routes → `200`, `source:"huggingface"`, real model attribution (`dslim/bert-base-NER`, `sentence-transformers/all-MiniLM-L6-v2`, `sshleifer/distilbart-cnn-12-6`) — **passed**
- T7: `api.logout()` clears localStorage token; same 3 AI routes → `401 "Authentication required"` again — **passed**

No `pageerror` events captured. The only `console.error` lines in the run are Chrome's automatic logging of the *deliberate* 401 responses from T5 and T7 (one per fetch × six fetches), plus one for the `400` returned mid-run before I corrected the match payload — **not regressions**, this is the browser surfacing intended HTTP status codes.

## Probe-level evidence (extracted from `run.log`)

```
T1  hasMotion=true  motionKeys=[animate,stagger,inView,scroll]
    legacyMotionImport=false  newMotionImport=true  motionErrors=0

T2  cardCount=3
    cardTitles=["Cloud Platform Engineer","Junior Data Scientist","Senior React Engineer"]
    jobIds=["69fe00c20f7dbb9b85e821c7","69fe00c20f7dbb9b85e821c8","69fe00c20f7dbb9b85e821c6"]
    noArgOpenCalls=0  matchesExactDemoTitle=false

T3a nameDisplay=flex  companyDisplay=none
T3b POST /api/auth/register -> 201
    body  = { name, email, password, role:"seeker" }    ← no "company"
    token = present in localStorage["giu-nexus.token"]

T4-pre nameDisplay=flex  companyDisplay=flex
T4a    companyHasError=true  errorMsgVisible=true  registerCallFired=false
T4b    POST /api/auth/register -> 201
       body = { name, email, password, role:"recruiter", company:"Adversary Holdings Ltd" }
       respUserRole = "recruiter"

T5  /ai/skills/extract -> 401 "Authentication required"
    /ai/match          -> 401 "Authentication required"
    /ai/summarize      -> 401 "Authentication required"

T6  /ai/skills/extract -> 200  source=huggingface  model=dslim/bert-base-NER             skillsLen=7
    /ai/match          -> 200  source=huggingface  model=sentence-transformers/all-MiniLM-L6-v2  matchesLen=3
    /ai/summarize      -> 200  source=huggingface  model=sshleifer/distilbart-cnn-12-6

T7  tokenAfterLogout=null
    /ai/skills/extract -> 401 "Authentication required"
    /ai/match          -> 401 "Authentication required"
    /ai/summarize      -> 401 "Authentication required"
```

## Visual evidence

### T2 — Jobs board, dynamic cards only

3 dynamic cards from Mongo (Cloud Platform Engineer, Junior Data Scientist, Senior React Engineer). The "Score these jobs against my CV" panel is the new HF match-scoring UI from PR #5 wired into the same grid. No demo cards remain.

![jobs board with only 3 dynamic cards](https://app.devin.ai/attachments/67459fff-ea56-46b6-b8f5-13d81eea427f/01-jobs-board.png)

### T6 — Authenticated AI endpoints, real Hugging Face attribution

Best-source-of-truth visual is the Milestone 2 demo recording I attached previously — it walks through all three endpoints with the model-attribution status lines visible in the UI:
- `Extracted N skills via dslim/bert-base-NER.` (or `jjzha/jobbert_skill_extraction.`)
- `Scored N jobs via sentence-transformers/all-MiniLM-L6-v2.`
- `Generated by sshleifer/distilbart-cnn-12-6.`

The same endpoints were re-hit in this run with a real JWT (T6 above) and produced 200 + `source:"huggingface"` + the same model names — no fallback path triggered.

## Things to flag

- **Visual artifact in static screenshots, not a bug.** The full-page background `<canvas>` (the orange/teal icosahedron) is `position:fixed` and renders on top of `auth-view` in the static screenshots I tried to capture for the recruiter / seeker forms — so those screenshots came back nearly black. The DOM was correct: probes confirm `auth-name-group` and `auth-company-group` had the right `computedStyle.display` values, the inputs accepted `setValue + dispatch input`, and the `signin-btn` click triggered the right network calls. The recording captures the in-page activity for those tests. This is a pre-existing layout quirk, not a regression introduced by PR #7.
- **HF token still in chat history.** Please rotate `HF_API_TOKEN` at https://huggingface.co/settings/tokens (Manage → Invalidate and refresh) once you've merged.

## How to reproduce

The runner and bootstrap helpers live under `tests/e2e/` (added in PR #8). From the repo root:

```bash
cd Backend && npm install && cd ..                       # one-time
HF_API_TOKEN=hf_xxx node tests/e2e/dev-bootstrap.js      # backend on :5000, seeds 3 jobs
(cd Frontend && python3 -m http.server 8080) &           # frontend on :8080
node tests/e2e/run-tests.js                              # 7-test adversarial run
```

The runner connects to a local Chrome that exposes CDP on `http://localhost:29229`. See [`tests/e2e/README.md`](../../tests/e2e/README.md) for full prerequisites.
